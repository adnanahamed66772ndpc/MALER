<UserControl x:Class="MailerApp.Desktop.Views.CampaignsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="340"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <TextBlock Text="Campaigns" Style="{StaticResource PageTitle}"/>
            <Border Style="{StaticResource Card}" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Button Grid.Row="0" Content="Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource SecondaryButton}"
                            HorizontalAlignment="Left" Margin="20,16,0,12"/>
                    <DataGrid Grid.Row="1" ItemsSource="{Binding Campaigns}" SelectedItem="{Binding SelectedCampaign}"
                              Style="{StaticResource ModernDataGrid}" AutoGenerateColumns="False" IsReadOnly="True" MinHeight="200">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="90"/>
                            <DataGridTextColumn Header="Sent" Binding="{Binding SentCount}" Width="60"/>
                            <DataGridTextColumn Header="Failed" Binding="{Binding FailedCount}" Width="60"/>
                            <DataGridTextColumn Header="Total" Binding="{Binding TotalRecipients}" Width="60"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </StackPanel>
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" Margin="24,0,0,0">
            <StackPanel>
                <TextBlock Text="Create campaign" Style="{StaticResource PageTitle}"/>
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Name" Style="{StaticResource FieldLabel}"/>
                        <TextBox Text="{Binding NewName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Margin="0,0,0,10"/>
                        <TextBlock Text="Template" Style="{StaticResource FieldLabel}"/>
                        <ComboBox ItemsSource="{Binding Templates}" SelectedValue="{Binding SelectedTemplateId}" SelectedValuePath="Id" DisplayMemberPath="Name" Style="{StaticResource ModernComboBox}" Margin="0,0,0,10"/>
                        <TextBlock Text="Sender account" Style="{StaticResource FieldLabel}"/>
                        <ComboBox ItemsSource="{Binding Senders}" SelectedValue="{Binding SelectedSenderId}" SelectedValuePath="Id" DisplayMemberPath="Email" Style="{StaticResource ModernComboBox}" Margin="0,0,0,10"/>
                        <TextBlock Text="Daily cap" Style="{StaticResource FieldLabel}"/>
                        <TextBox Text="{Binding DailyCap, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Margin="0,0,0,10"/>
                        <TextBlock Text="Delay min (ms)" Style="{StaticResource FieldLabel}"/>
                        <TextBox Text="{Binding DelayMin, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Margin="0,0,0,10"/>
                        <TextBlock Text="Delay max (ms)" Style="{StaticResource FieldLabel}"/>
                        <TextBox Text="{Binding DelayMax, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Margin="0,0,0,16"/>
                        <Button Content="Create campaign" Command="{Binding CreateCommand}" Style="{StaticResource PrimaryButton}"/>
                    </StackPanel>
                </Border>
                <TextBlock Text="Start campaign" Style="{StaticResource PageTitle}"/>
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Recipients from list" Style="{StaticResource FieldLabel}"/>
                        <ComboBox ItemsSource="{Binding Lists}" SelectedValue="{Binding SelectedListId}" SelectedValuePath="Id" DisplayMemberPath="Name" Style="{StaticResource ModernComboBox}" Margin="0,0,0,10"/>
                        <TextBlock Text="Senders with caps (optional)" Style="{StaticResource FieldLabel}"/>
                        <TextBlock Text="Leave empty to use the campaign&#x0a;s single sender. Add rows to split sends: e.g. 250 from one, 200 from another." TextWrapping="Wrap" Margin="0,0,0,6" Foreground="#666" FontSize="12"/>
                        <ItemsControl ItemsSource="{Binding SenderCaps}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <ComboBox Grid.Column="0" ItemsSource="{Binding DataContext.Senders, RelativeSource={RelativeSource AncestorType=UserControl}}" SelectedValue="{Binding SenderAccountId}" SelectedValuePath="Id" DisplayMemberPath="Email" Style="{StaticResource ModernComboBox}" Margin="0,0,8,0"/>
                                        <TextBox Grid.Column="1" Text="{Binding MaxEmails, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Margin="0,0,8,0"/>
                                        <Button Grid.Column="2" Content="Remove" Command="{Binding DataContext.RemoveSenderCapCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource SecondaryButton}"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <Button Content="Add sender + cap" Command="{Binding AddSenderCapCommand}" Style="{StaticResource SecondaryButton}" HorizontalAlignment="Left" Margin="0,0,0,16"/>
                        <StackPanel Orientation="Horizontal">
                            <Button Content="Start" Command="{Binding StartCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,10,0"/>
                            <Button Content="Pause" Command="{Binding PauseCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0"/>
                            <Button Content="Stop" Command="{Binding StopCommand}" Style="{StaticResource DangerButton}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
