<UserControl x:Class="MailerApp.Desktop.Views.ContactsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <TextBlock Text="Contacts" Style="{StaticResource PageTitle}"/>
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="Contact lists" Style="{StaticResource SectionTitle}"/>
                    <Button Content="Refresh lists" Command="{Binding RefreshListsCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,0,12"/>
                    <TextBlock Text="New list name" Style="{StaticResource FieldLabel}"/>
                    <TextBox Text="{Binding NewListName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Margin="0,0,0,8"/>
                    <TextBlock Text="Description (optional)" Style="{StaticResource FieldLabel}"/>
                    <TextBox Text="{Binding NewListDescription, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" TextWrapping="Wrap" AcceptsReturn="True" Height="50" VerticalScrollBarVisibility="Auto" Margin="0,0,0,12"/>
                    <Button Content="Create list" Command="{Binding CreateListCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,0,16"/>
                    <TextBlock Text="Select list" Style="{StaticResource FieldLabel}"/>
                    <ListBox ItemsSource="{Binding Lists}" SelectedItem="{Binding SelectedList}" DisplayMemberPath="Name"
                             BorderThickness="0" Background="#f8fafc" Padding="8" Margin="0,0,0,12" MinHeight="100"/>
                    <Button Content="Import CSV" Command="{Binding ImportCsvCommand}" Style="{StaticResource PrimaryButton}"/>
                </StackPanel>
            </Border>
        </StackPanel>
        <StackPanel Grid.Column="1" Margin="24,0,0,0">
            <TextBlock Text="Contacts in list" Style="{StaticResource PageTitle}"/>
            <Border Style="{StaticResource Card}" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Button Grid.Row="0" Content="Refresh" Command="{Binding RefreshContactsCommand}" Style="{StaticResource SecondaryButton}"
                            HorizontalAlignment="Left" Margin="20,16,0,12"/>
                    <DataGrid Grid.Row="1" ItemsSource="{Binding Contacts}" Style="{StaticResource ModernDataGrid}"
                              AutoGenerateColumns="False" IsReadOnly="True" MinHeight="280" Margin="0,0,0,0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                            <DataGridTextColumn Header="First name" Binding="{Binding FirstName}" Width="120"/>
                            <DataGridTextColumn Header="Last name" Binding="{Binding LastName}" Width="120"/>
                            <DataGridTextColumn Header="Company" Binding="{Binding Company}" Width="140"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
